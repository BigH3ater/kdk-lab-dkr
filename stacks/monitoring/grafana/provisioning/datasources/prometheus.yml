# Provisioned, not clicked: the datasource is in git so a rebuilt Pi comes
# back with Grafana already pointed at its own Prometheus.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus-ext
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
