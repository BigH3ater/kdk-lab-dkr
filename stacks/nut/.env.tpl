# Rendered by the Komodo pre_deploy step: op inject -i .env.tpl -o .env
# upsd 'monuser' password — the Home Assistant NUT client authenticates with it.
NUT_CLIENT_PASSWORD=op://kdk-ops/kdk-ups-01-nut-upsd/password
