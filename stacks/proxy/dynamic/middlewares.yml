http:
  middlewares:
    forwardauth:
      forwardAuth:
        address: "https://sso.kmkdp.com/api/authz/forward-auth"
        trustForwardHeader: true
        authResponseHeaders:
          - Remote-User
          - Remote-Groups
          - Remote-Email
          - Remote-Name
